<h1>Hello, Rails!</h1>

<%= Article.public_count %> datas on db!

<ul>
    <% @articles.each do |article| %>
        <% unless article.archived? %>
            <li>
                <%= link_to article.title, article %>
            </li>
        <% end %>
    <% end %>
</ul>
</ul>

<%= link_to "Create", new_article_path %>
